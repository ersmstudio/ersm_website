# ✅ Framework الأساسي
fastapi==0.111.0

# ✅ سيرفر سريع غير متزامن (مع دعم watchgod و websockets و httptools)
uvicorn[standard]==0.30.1

# ✅ ORM للتعامل مع قواعد البيانات
sqlalchemy==2.0.30

# ✅ PostgreSQL Driver
psycopg2-binary==2.9.9

# ✅ لتحميل المتغيرات من ملفات .env
python-dotenv==1.0.1

# ✅ Jinja2 للتعامل مع قوالب HTML (لو فيه صفحات front-end)
jinja2==3.1.4

# ✅ aiofiles لتخزين الملفات (صور، مستندات، إلخ)
aiofiles==23.2.1

# ✅ python-multipart لمعالجة الـ form data في الملفات أو الصور
python-multipart==0.0.9

# ✅ passlib للتشفير الآمن لكلمات المرور (يدعم bcrypt وغيره)
passlib[bcrypt]==1.7.4

# ✅ jose للتعامل مع JWT tokens (للمصادقة)
python-jose==3.3.0

# ✅ email-validator لتأكيد صحة الإيميلات
email-validator==2.1.1

# ✅ httpx للتعامل مع طلبات HTTP غير متزامنة
httpx==0.27.0
# ✅ pydantic لتأكيد صحة البيانات (موجودة في FastAPI لكن يمكن تحديثها)
pydantic==2.7.4